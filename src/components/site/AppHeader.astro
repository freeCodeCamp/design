---
import ThemeOptions from './ThemeOptions.astro';
const current = Astro.url.pathname.replace(/\/$/, '') || '/';
const links = [
  ['/', 'Overview'],
  ['/handbook', 'Style guide'],
  ['/playground', 'Playground']
];
---

<header class='site-header'>
  <div class='wrap header-inner'>
    <a class='brand-link' href='/' aria-label='freeCodeCamp Design home'>
      <img
        class='brand-wordmark brand-wordmark-light'
        src='/brand/fcc-primary.svg'
        width='210'
        height='24'
        alt='freeCodeCamp'
      />
      <img
        class='brand-wordmark brand-wordmark-dark'
        src='/brand/fcc-secondary.svg'
        width='210'
        height='24'
        alt='freeCodeCamp'
      />
      <img
        class='brand-glyph'
        src='/brand/fcc-puck.svg'
        width='24'
        height='24'
        alt='freeCodeCamp'
      />
      <span>Design</span>
    </a>
    <nav aria-label='Main navigation'>
      {
        links.map(([href, label]) => (
          <a href={href} aria-current={current === href ? 'page' : undefined}>
            {label}
          </a>
        ))
      }
    </nav>
    <ThemeOptions />
  </div>
</header>
